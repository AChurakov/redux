require "./redux/*"

module Redux
  # class CombineReducers
  #   def initialize(reducers, state, action)
  #     reducers.to_h.reduce(state) { |next_state, (key, reducer)|
  #       puts state
  #       # next_state[key] = reducer.call(state, action)
  #       # next_state
  #     }
  #   end
  # end
end
